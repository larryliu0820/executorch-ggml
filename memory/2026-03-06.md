
## Parakeet ggml debugging (Larry) — 2026-03-06

- Rebuilt `/tmp/parakeet-ggml` and confirmed unit tests pass (`pytest -q tests`: 80 passed, 2 skipped).
- Running real audio via `third-party/executorch/examples/models/parakeet/export_parakeet_tdt.py --backend ggml --audio /Volumes/larryliu/work/executorch/test_audio.wav` fails at runtime.

### Failures observed
- Initially failed in portable `aten::logical_and.out` due to non-broadcastable transposed shapes: `(1,63,625)` vs `(1,625,63)`.
- After delegating logical_and to ggml, failure moved to delegated `BITWISE_AND` with shape mismatch (mixed time bases): `a=(625,63,1,1)` vs `b=(63,63,1,1)`.
- Claude Code later reproduced a similar portable failure case: `(1,79,625)` vs `(1,625,79)` (T_enc=79 ~ 625/8).

### Changes made in /tmp/parakeet-ggml
- Added slow E2E regression test file: `tests/test_parakeet_tdt_audio.py` (gated by `RUN_PARKEET_TDT_AUDIO_TEST=1`, optional `PARAKEET_TDT_AUDIO_PATH`), registered `slow` marker in `pyproject.toml`. Test currently marked xfail until runtime works.
- Added logical_and support to ggml backend:
  - `python/executorch_ggml/ggml_partitioner.py`: added `aten.logical_and.{default,out}` supported.
  - `python/executorch_ggml/ggml_backend.py`: lower `aten.logical_and.*` -> `OP_BITWISE_AND`.
  - `runtime/ggml_backend.cpp`: extended `OpCode_BITWISE_AND` to attempt broadcast/permute alignment and improved trace logging.
- Fixed a real lowering bug: permute lowering was hardcoding output dtype to F32. Updated `aten.permute{,_copy}` lowering to preserve dtype from `fake_val.dtype`.
- Added runtime tracing helpers controlled by `GGML_TRACE_SHAPES=1` and optional `GGML_TRACE_TIDS=...`.

### Debug artifacts
- Progress writeup: `/Users/mengweiliu/clawd/parakeet_ggml_progress_2026-03-06.md`.
- tmux session started for Claude Code debugging: `tmux session claude_code` in `/tmp/parakeet-ggml`.

## OpenClaw model request: gpt-5.4
- Larry referenced PR adding gpt-5.4 support: https://github.com/openclaw/openclaw/pull/36590.
- Current OpenClaw version installed: 2026.3.2 (npm global). npm dist-tags show only latest=2026.3.2 and beta=2026.3.2-beta.1; no `dev` tag available.
- Added `gpt-5.4` entry to `~/.openclaw/openclaw.json` openai provider models, but `openclaw models list` did not show it (likely because binary/UI allowlists don’t include it yet).
- Attempted `openclaw update --channel dev` failed due to OPENCLAW_GIT_DIR non-git dir; `openclaw update --tag dev` failed because no npm version `openclaw@dev`.

## Parakeet ggml re-export + rerun (later 2026-03-06)
- Installed `nemo_toolkit[asr]` to enable export; this downgraded `transformers` to 4.57.6 and created a dependency conflict with `optimum-executorch` (expects `transformers==5.0.0rc1`).
- Fixed `runtime/ggml_backend.cpp` WHERE lowering: removed use of non-existent `ggml_where`; implemented as `mask*fill + (1-mask)*x`.
- Re-exported Parakeet TDT with ggml backend:
  - `/tmp/parakeet-ggml/parakeet_tdt_ggml_exports_reexport_20260306_222318/model.pte` (+ `tokenizer.model`).
- Re-ran encoder with `GGML_TRACE_SHAPES=1`: failure occurs inside ggml delegate with
  - `BITWISE_AND shape mismatch not broadcastable: a=(625,79,1,1) b=(79,79,1,1)`
  indicating mixed time bases (T_mel vs T_enc) in mask combine.

## Preference / workflow note
- Larry requested: next time run ExecuTorch/parakeet export + ggml tests inside conda env `executorch`.

## Parakeet ggml debugging — later updates (2026-03-06)

- Implemented (then reverted) experimental runtime “fixes” in `runtime/ggml_backend.cpp` to handle Parakeet mask shape mismatches by cropping/broadcasting in `BITWISE_AND` and `WHERE`. These progressed execution but then hit a new ggml assert in `im2col` requiring F16 (`GGML_ASSERT(src0->type == GGML_TYPE_F16)`). Larry requested no hacks; reverted all tracked changes.
- After revert, verified Qwen3 tests still pass:
  - `pytest -q tests/test_qwen3_optimum.py tests/test_qwen3_one_layer_gqa_prefill.py tests/test_qwen3_numerical.py`
  - Result: **4 passed** (warnings only), ~1m41s.
- Repo state after revert: no tracked changes; only untracked debug artifacts remained (exports, scripts, `qwen3_dynamic_export.pte`, etc.).
